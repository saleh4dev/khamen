services:
  - type: web
    name: who-game
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: CORS_ORIGIN
        sync: false
      - key: NODE_VERSION
        value: "20"
